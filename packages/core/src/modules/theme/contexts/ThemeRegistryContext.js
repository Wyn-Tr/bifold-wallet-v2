/**
 * Theme Registry Context
 *
 * Separate file to avoid circular imports.
 */
import { createContext } from 'react';
/**
 * Theme Registry Context
 * Used by components to access the theme registry
 */
export const ThemeRegistryContext = createContext(undefined);
