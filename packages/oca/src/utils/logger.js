export default class MiniLogger {
    warn(message) {
        // eslint-disable-next-line no-console
        console.log(message);
    }
    error(message) {
        // eslint-disable-next-line no-console
        console.log(message);
    }
    info(message) {
        // eslint-disable-next-line no-console
        console.log(message);
    }
}
